<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <h1>I am here</h1>

  <% if (users.length > 0) { %>
    <% users.forEach(user => { %>

      <h1><%= user.username %></h1>
    <% }) %>
  <% } %>
</body>
</html>